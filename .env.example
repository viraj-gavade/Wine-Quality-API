# Example environment variables
# Path to the processed CSV used for training/testing
DATA_PATH=data/proccessed_winequality.csv

# Directory where models are saved/loaded (relative to project root)
MODEL_DIR=Models/trained

# Server settings
UVICORN_HOST=0.0.0.0
UVICORN_PORT=8000
